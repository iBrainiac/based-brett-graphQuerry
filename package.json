{
  "name": "base-name-querry",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ base-name-querry",
    "create-local": "graph create --node http://localhost:8020/ base-name-querry",
    "remove-local": "graph remove --node http://localhost:8020/ base-name-querry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 base-name-querry",
    "test": "graph test"
  },
  "dependencies": {
    "@apollo/client": "^3.11.8",
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
